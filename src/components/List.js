import React from 'react';
import PokeCard from './PokeCard.js';

const List = ({pokemonUrls}) => {
  if (!pokemons || pokemons.length === 0) return <p>No repos, sorry</p>;
  
  return(
    console.log(<h1>hello world</h1>)
  );
}

export default List;